# SG-OutBack-PS
Simple Outlook Backup PowerShell script that will back up a particular folder with .pst files (even if they're in use - it uses Microsoft VSS) to a desired location, then emails a report
